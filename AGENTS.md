# AGENTS.md - E:\store_wechatAPP 项目级上下文

## 适用范围

本文件只适用于 `E:\store_wechatAPP` 及其子目录下的家庭库存管理微信小程序项目。

它是当前项目的任务前上下文和执行约束，不是其他项目、其他仓库或整台机器的通用规则。处理本项目任务前，先阅读本文件，再阅读与任务相关的代码和文档。

## 任务前必读

- 当前项目是家庭库存管理微信小程序，前端为微信原生小程序 + TypeScript，后端使用微信云开发云函数。
- 当前代码以本地缓存为主要数据源，云端能力主要用于提醒和手动备份快照。
- README 面向项目介绍；本文件面向执行上下文、协作约束、当前状态和长期决策。
- 工作区可能存在用户或前序任务留下的未提交改动。修改前先确认相关文件状态，不要回退、覆盖或清理无关改动。
- 优先保持现有项目结构和微信小程序原生实现方式，不引入不必要的新框架或大规模重构。

## 硬性文件操作规则

禁止批量删除文件或目录。

不要使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

需要删除文件时，只能一次删除一个明确路径的文件。

正确示例：

```powershell
Remove-Item "C:\path\to\file.txt"
```

如果需要批量删除文件，应停止操作，并请求用户手动删除。

## 当前项目状态

- 已有页面包括：首页、库存页、提醒页、账单页、记账页、编辑页、详情页、单物品账单页、周期页、我的页、备份页、恢复页、分类管理页、位置管理页、单位管理页、关于页。
- 核心数据层集中在 `miniprogram/services/store.ts`，包含仓库、物品、账单、位置、分类、单位、引导状态、备份快照和恢复逻辑。
- 页面同时存在 `.ts` 和编译后的 `.js` 文件。修改 TypeScript 逻辑时，需要确认是否也要同步对应 `.js`，避免微信开发者工具运行到旧逻辑。
- 云函数当前包括 `dailyReminder` 和 `storeBackup`，分别服务提醒和云端备份。
- 当前第一版库存预测规则仍以购买周期、平台默认送达时间和本地记录为核心，不要在未确认产品意图前改成复杂预测模型。
- 仓库删除采用软删除：从正常仓库列表隐藏，保留完整物品、账单和基础资料，可从“我的-已删除仓库”恢复。

## 当前正在做什么

- 维护一个可持续更新的项目级上下文文件，让后续每次任务开始时能快速理解当前项目目标和约束。
- 将 README 中偏项目介绍的信息与 AGENTS.md 中偏执行约束的信息分开维护。
- 后续如继续开发功能，应优先围绕备份恢复可靠性、管理页体验、库存/周期规则和新手引导一致性展开。

## 执行规则

- 每次任务开始先读本文件，再按任务范围读取相关代码。
- 只做用户请求范围内的改动，不顺手重构无关模块。
- 涉及数据结构、备份格式、恢复逻辑、云函数入参出参时，要先确认兼容性影响。
- 涉及页面路由时，同步检查 `miniprogram/app.json`。
- 涉及用户可见文案时，保持中文表达简洁、直接，符合家庭库存管理场景。
- 涉及 UI 时，保持现有小程序风格一致，避免引入营销页式布局。
- 任务完成后，如有长期价值的项目状态或决策变化，再更新本文件。

## 维护纪律

- 本文件采用“混合索引”：前半部分保持任务前快速摘要，后半部分记录关键决策。
- 不记录普通 bug 修复、临时排查过程、样式微调流水账。
- “当前正在做什么”最多保留 3 条活跃事项，完成后删除或转入关键决策记录。
- 只有项目方向、硬性规则、核心数据模型、重要功能状态或长期协作方式变化时，才更新本文件。
- 如果本文件明显膨胀，先压缩摘要；仍然过长时，再考虑拆出单独的 `PROJECT_CONTEXT.md`，不要默认拆分。

## 关键决策记录

### 2026-05-11

- 决定：在 `E:\store_wechatAPP\AGENTS.md` 建立项目级上下文文件。
- 原因：后续任务开始前需要快速明确当前项目目标、执行约束和长期决策，避免重复追溯聊天记录。
- 影响：本文件只约束 `E:\store_wechatAPP` 项目；其他路径下的项目需要单独设计自己的上下文文件。

### 2026-05-11

- 决定：本文件采用“混合索引”而不是完整历史档案。
- 原因：任务前必读内容必须短而可执行，同时保留少量关键决策用于追溯。
- 影响：后续维护时只写长期有效的信息，不把聊天过程或普通修改记录写成流水账。

### 2026-05-12

- 决定：仓库删除采用软删除，不提供彻底删除入口；当删除最后一个正常仓库时，自动创建一个新的空仓库作为当前仓库。
- 原因：家庭库存和账单历史需要可找回，同时现有页面假设始终存在当前仓库。
- 影响：`InventoryStore` 可包含 `deletedHouses`；备份快照应携带已删除仓库，恢复入口保留在“我的”页。
